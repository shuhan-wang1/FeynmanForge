# FeynmanForge v4.1 - 多画布管理 & 多图表生成

## 🎉 新功能

### 1. 多画布管理系统
- ✅ **创建新画布**：点击顶部的 "+" 按钮
- ✅ **切换画布**：点击当前画布名称，选择要切换的画布
- ✅ **重命名画布**：在画布列表中点击编辑图标
- ✅ **复制画布**：快速复制当前画布
- ✅ **删除画布**：删除不需要的画布（至少保留一个）
- ✅ **自动保存**：每次修改自动保存到 LocalStorage
- ✅ **导出/导入**：支持导出所有画布为 JSON 文件备份

### 2. 多图表生成（Gemini AI）
- ✅ **生成所有费曼图**：勾选"生成所有可能的费曼图"选项
- ✅ **自动创建画布**：每个图表（t-channel, u-channel, s-channel 等）自动创建独立画布
- ✅ **智能命名**：画布自动命名为"反应式 - 图表类型"

## 📝 使用指南

### 基本工作流

1. **输入反应式**（例如：`$u_red$ + $ubar_anti-red$ $->$ $gluon$ + $gluon$`）
2. **勾选"生成所有可能的费曼图"**（如果想要多个图表）
3. **点击"生成费曼图"**
4. **AI 自动生成**：
   - 如果有多个图表（如 t-channel, u-channel），会为每个创建新画布
   - 可以在顶部画布列表中切换查看不同的图表

### 画布管理

#### 创建新画布
```
点击顶部 "+" 按钮 → 输入画布名称 → 开始绘制
```

#### 切换画布
```
点击画布名称下拉菜单 → 选择目标画布 → 自动加载
```

#### 导出所有画布
```
打开画布菜单 → 点击"导出全部" → 下载 JSON 文件
```

#### 导入画布
```
打开画布菜单 → 点击"导入" → 选择之前导出的 JSON 文件
```

## 🔧 技术实现

### 架构设计

```
canvas-manager.js          # 核心画布管理类
├── CanvasManager          # 管理多个画布的数据结构
├── 保存到 LocalStorage     # 自动持久化
└── 触发更新事件           # 通知 UI 刷新

canvas-manager-ui.js       # UI 交互逻辑
├── 渲染画布列表
├── 处理切换/重命名/删除
└── 导出/导入功能

gemini-integration.js      # AI 生成逻辑（更新）
├── generateFeynmanDiagram # 支持 { generateAll: true } 选项
├── 返回单个或数组结果
└── 为每个图表添加名称

feynman-logic.js           # 主逻辑（更新）
├── loadShapes()           # 加载画布数据
├── clearAllShapes()       # 清空画布
└── 自动保存到画布管理器
```

### 数据结构

```javascript
// 画布对象
{
  id: 1,
  name: "u + ū → gg - t-channel",
  shapes: [...],  // 所有绘制的形状
  createdAt: "2025-11-15T10:00:00.000Z",
  updatedAt: "2025-11-15T10:05:00.000Z"
}

// Gemini 多图表响应
{
  diagrams: [
    { name: "t-channel", vertices: [...], lines: [...] },
    { name: "u-channel", vertices: [...], lines: [...] },
    { name: "s-channel", vertices: [...], lines: [...] }
  ]
}
```

## 🎨 UI 更新

### 顶部工具栏
```
[返回主页] [🔬 FeynmanForge v4.1] [📄 当前画布名 ▼] [➕ 新建]
```

### 画布列表菜单
```
┌─────────────────────────────────┐
│ 📄 画布管理                      │
├─────────────────────────────────┤
│ ✅ Diagram 1 (当前)              │
│    3 shapes • 11/15 10:00       │
│    [→] [✏️] [📋] [🗑️]           │
├─────────────────────────────────┤
│ 📄 u+ū→gg - t-channel           │
│    5 shapes • 11/15 10:05       │
│    [→] [✏️] [📋] [🗑️]           │
├─────────────────────────────────┤
│ [导出全部] [导入]                │
└─────────────────────────────────┘
```

### AI 生成面板（新增选项）
```
☑ 生成所有可能的费曼图
   (如 t-channel, u-channel, s-channel)
```

## 🚀 使用示例

### 示例 1：生成单个图表
```
输入: $e-$ + $e+$ $->$ $mu-$ + $mu+$
选项: ☐ 生成所有可能的费曼图
结果: 当前画布显示 s-channel 图表
```

### 示例 2：生成多个图表
```
输入: $u_red$ + $ubar_anti-red$ $->$ $gluon$ + $gluon$
选项: ☑ 生成所有可能的费曼图
结果: 
  - 画布 1: "u+ū→gg - t-channel"
  - 画布 2: "u+ū→gg - u-channel"
  - 画布 3: "u+ū→gg - s-channel" (可能)
```

### 示例 3：管理多个项目
```
画布 1: "电子散射实验"
画布 2: "夸克强相互作用"
画布 3: "弱衰变过程"
...随时切换，自动保存
```

## 💡 提示

1. **自动保存**：每次修改自动保存，无需手动操作
2. **多图表生成**：强相互作用（夸克-胶子）通常有多个图表
3. **画布命名**：建议使用清晰的命名，方便管理
4. **定期备份**：使用"导出全部"功能定期备份重要工作

## 🐛 故障排除

### 画布列表为空
- 检查浏览器 LocalStorage 是否启用
- 尝试刷新页面重新加载

### 切换画布后内容不变
- 检查浏览器控制台是否有错误
- 确保已正确加载 canvas-manager.js 和 canvas-manager-ui.js

### AI 生成失败
- 确认已保存有效的 Gemini API Key
- 检查网络连接
- 查看控制台详细错误信息

## 📚 相关文件

- `canvas-manager.js` - 核心管理类
- `canvas-manager-ui.js` - UI 交互
- `gemini-integration.js` - AI 生成（已更新）
- `feynman-logic.js` - 主逻辑（已更新）
- `feymann.html` - UI 界面（已更新）

---

**版本**: v4.1  
**日期**: 2025-11-15  
**作者**: FeynmanForge Team
